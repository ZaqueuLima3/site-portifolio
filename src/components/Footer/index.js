import React from "react";

import MaterialIcon, { colorPalette } from "material-icons-react";

import { Container } from "./styles";

const Footer = () => (
  <Container>
    <MaterialIcon icon="dashboard" color="#FFF" size={45} />
  </Container>
);

export default Footer;
